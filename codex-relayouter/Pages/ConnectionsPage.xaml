<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="codex_bridge.Pages.ConnectionsPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:codex_bridge.ViewModels"
    mc:Ignorable="d">

    <Grid Padding="24" RowDefinitions="Auto,Auto,*" RowSpacing="16">

        <TextBlock Text="连接" Style="{StaticResource TitleTextBlockStyle}" />

        <Border
            Grid.Row="1"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
            BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
            BorderThickness="1"
            CornerRadius="8"
            Padding="16">
            <StackPanel Spacing="12">
                <StackPanel Orientation="Horizontal" Spacing="12">
                    <ToggleSwitch
                        x:Name="LanEnabledToggle"
                        Header="允许局域网连接"
                        Toggled="LanEnabledToggle_Toggled" />
                    <TextBlock x:Name="StatusTextBlock" VerticalAlignment="Center" Opacity="0.7" />
                </StackPanel>

                <Grid ColumnDefinitions="Auto,*" ColumnSpacing="12">
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorSecondaryBrush}"
                        CornerRadius="8"
                        Padding="12">
                        <Image x:Name="QrImage" Width="180" Height="180" Stretch="Uniform" />
                    </Border>

                    <StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="12">
                        <TextBlock Text="配对信息" Style="{StaticResource BodyStrongTextBlockStyle}" />
                        <TextBlock
                            Opacity="0.6"
                            Text="扫描二维码或点击下方按钮复制配对链接"
                            TextWrapping="Wrap" />
                        <Button x:Name="CopyPairingButton" Click="CopyPairingButton_Click" Style="{StaticResource AccentButtonStyle}">
                            <StackPanel Orientation="Horizontal" Spacing="6">
                                <SymbolIcon Symbol="Copy" />
                                <TextBlock Text="复制配对链接" />
                            </StackPanel>
                        </Button>
                        <TextBlock
                            Opacity="0.5"
                            Text="提示：扫码/输入后仍需在本机确认；撤销设备可立即断开并失效。"
                            TextWrapping="Wrap" />
                    </StackPanel>
                </Grid>
            </StackPanel>
        </Border>

        <ListView
            x:Name="DevicesListView"
            Grid.Row="2"
            ItemsSource="{x:Bind Devices, Mode=OneWay}"
            SelectionMode="None">
            <ListView.ItemContainerStyle>
                <Style TargetType="ListViewItem">
                    <Setter Property="HorizontalContentAlignment" Value="Stretch" />
                    <Setter Property="Padding" Value="0,4" />
                </Style>
            </ListView.ItemContainerStyle>
            <ListView.ItemTemplate>
                <DataTemplate x:DataType="vm:PairedDeviceViewModel">
                    <Border
                        Background="{ThemeResource CardBackgroundFillColorDefaultBrush}"
                        BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                        BorderThickness="1"
                        CornerRadius="8"
                        Padding="16,12">
                        <Grid ColumnDefinitions="Auto,*,Auto" ColumnSpacing="12">
                            <FontIcon Glyph="&#xE8D4;" Opacity="0.7" VerticalAlignment="Center" />

                            <StackPanel Grid.Column="1" Spacing="4">
                                <TextBlock Text="{x:Bind Title}" Style="{StaticResource BodyStrongTextBlockStyle}" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind Subtitle}" Opacity="0.7" TextTrimming="CharacterEllipsis" />
                                <TextBlock Text="{x:Bind StatusText}" Opacity="0.6" TextWrapping="Wrap" />
                                <TextBlock Text="{x:Bind DeviceId}" Opacity="0.5" FontSize="12" FontFamily="Cascadia Code,Consolas,monospace" TextTrimming="CharacterEllipsis" />
                            </StackPanel>

                            <StackPanel Grid.Column="2" Spacing="8" VerticalAlignment="Center">
                                <Border
                                    Background="{ThemeResource SystemControlBackgroundAccentBrush}"
                                    CornerRadius="12"
                                    Padding="8,2"
                                    Visibility="{x:Bind IsOnline}">
                                    <TextBlock Text="在线" Foreground="White" FontSize="12" />
                                </Border>

                                <Button
                                    Tag="{x:Bind DeviceId}"
                                    Visibility="{x:Bind IsRevoked}"
                                    IsEnabled="False">
                                    <TextBlock Text="已撤销" />
                                </Button>

                                <Button
                                    Tag="{x:Bind DeviceId}"
                                    Visibility="{x:Bind CanRevoke}"
                                    Click="RevokeButton_Click">
                                    <TextBlock Text="撤销" />
                                </Button>
                            </StackPanel>
                        </Grid>
                    </Border>
                </DataTemplate>
            </ListView.ItemTemplate>
        </ListView>
    </Grid>
</Page>
